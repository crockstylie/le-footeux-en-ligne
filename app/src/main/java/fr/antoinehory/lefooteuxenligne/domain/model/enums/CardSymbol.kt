package fr.antoinehory.lefooteuxenligne.domain.model.enums

/**
 * Represents the special symbol on a game card (Main = Hand).
 */
enum class CardSymbol {
    MAIN, // Represents the 'Hand' symbol for stopping a penalty
    RED_FLAG,
    BLUE_FLAG,
    NONE // No specific symbol
}
