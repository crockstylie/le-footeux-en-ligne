package fr.antoinehory.lefooteuxenligne.domain.model.enums

/**
 * Represents a zone on the football field.
 */
enum class FieldZone {
    /** The center spot of the field. */
    CENTER_SPOT,

    /** The penalty area. */
    PENALTY_AREA,

    /** The goal line. */
    GOAL_LINE,

    /** The corner arc. */
    CORNER_ARC,

    /** The center circle. */
    CENTER_CIRCLE,

    /** The halfway line. */
    HALFWAY_LINE,

    /** The touchline. */
    TOUCHLINE,

    /** The goal area. */
    GOAL_AREA,

    /** The penalty spot. */
    PENALTY_SPOT,

    /** Outside the field. */
    OUT_OF_BOUNDS
}
